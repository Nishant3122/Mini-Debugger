# Mini-Debugger
Implement a minimal debugger for ELF binaries that can load, control, inspect, and step through processes using OS-level debugging primitives. The debugger must not rely on GDB or other external tools.
